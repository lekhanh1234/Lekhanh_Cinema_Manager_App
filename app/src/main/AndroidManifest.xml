<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CinemaManagerApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.ChangePassword"
            android:exported="false" />
        <activity
            android:name=".ui.activities.History"
            android:exported="false" />
        <activity
            android:name=".ui.activities.NowMovie"
            android:exported="true"
            android:theme="@style/Base.Theme.AppLight" />
        <activity
            android:name=".ui.activities.Payment"
            android:exported="true"
            android:theme="@style/Base.Theme.AppLight" />
        <activity
            android:name=".ui.activities.ChooseChair"
            android:exported="true"
            android:theme="@style/Base.Theme.AppLight" />
        <activity
            android:name=".ui.activities.accounts.AccountActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true"> <!-- Cập nhật ở đây -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.Movie"
            android:exported="true"
            android:theme="@style/Base.Theme.AppLight" />
    </application>

</manifest>